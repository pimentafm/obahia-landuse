import styled from 'styled-components';

export const PlotContainer = styled.div`
    & {
        top: 50px;
        right: 10px;
        flex-direction: column;
        position: absolute;
        z-index: 1;
    }
`;