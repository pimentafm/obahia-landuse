import styled from 'styled-components';

export const ReportContainer = styled.div`
    & {
        .pdf-viewer {
            position: absolute;
            width:100%;
            height: 100%;
            margin: 0;
            border:none;
        }
    }
`;